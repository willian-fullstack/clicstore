export default function DimensionBadges({ show }) {
  // Dimension badges are now rendered inside the 3D Scene using drei's <Html> 
  // so they automatically track the 3D object's position perfectly!
  return null;
}
